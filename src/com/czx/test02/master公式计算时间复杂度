T(n) = a · T(n/b) + f(n)
其中：
n：问题规模
a ≥ 1：每次递归调用的子问题数量
b > 1：每个子问题的规模是原问题的 1/b
f(n)：除递归外的其他操作（如划分、合并）的时间复杂度
✅ 要求：a 和 b 是常数，且子问题规模相等（即“均匀分治”）

时间复杂度:
1) log(b, a) > d -> 复杂度为O(N^log(b,a))
2) log(b, a) = d -> 复杂度为O(N^d * logN)
3) log(b, a) < d -> 复杂度为O(N^d)